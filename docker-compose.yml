services:
  spotify-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - spotify_tracker_data:/app/data
    restart: unless-stopped

volumes:
  spotify_tracker_data:
